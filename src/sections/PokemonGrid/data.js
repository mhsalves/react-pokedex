const emptyDescription = 'Sem Pokémon listados.';
const getImageAlt = (name) => `Esse é o ${name}`;
const getNumberLabel = (number) => `#${number}`;

export default {
  emptyDescription,
  getImageAlt,
  getNumberLabel,
}
